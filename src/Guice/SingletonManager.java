package Guice;

/**
 * @author xuch.
 */
// Scope annotations like Singleton, SessionScoped, RequestScoped are not allowed to annotate abstract types(abstract class and interface)
// @Singleton
public interface SingletonManager {
}
